{% extends 'base.html.twig' %}

{% block title %}
	{{ 'subject.edit.title' | trans }}
{% endblock %}
{% block page_title %}✏️
	{{ 'subject.edit.title' | trans }}
{% endblock %}

{% block body %}

	<div class="card p-4">
		{{ include('subject/_form.html.twig', {'button_label': 'subject.edit.button' | trans}) }}
		<a href="{{ path('app_subject_index') }}" class="btn btn-secondary rounded-3 mt-2">
			{{ 'subject.show.back' | trans }}</a>
		<hr>
		{{ include('subject/_delete_form.html.twig') }}
	</div>

{% endblock %}
